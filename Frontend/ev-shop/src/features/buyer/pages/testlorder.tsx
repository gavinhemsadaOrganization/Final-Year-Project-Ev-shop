import React from "react";
import { PageLoader } from "@/components/Loader";

const LoaderTestPage: React.FC = () => {
  // This component simply renders the full-screen PageLoader.
  return <PageLoader />;
};

export default LoaderTestPage;
